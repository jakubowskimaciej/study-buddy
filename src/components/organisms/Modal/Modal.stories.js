import Modal from './Modal';

export default {
  title: 'Components/Organisms/Modal',
  component: Modal,
};

const Template = (args) => <Modal {...args}>Read</Modal>;
export const Default = Template.bind({});
