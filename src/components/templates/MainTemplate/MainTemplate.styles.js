import styled from 'styled-components';

export const Wrapper = styled.div`
  width: 100%;
  height: 100vh;
  display: grid;
  grid-template-columns: 11rem 1fr 0.75fr;
  grid-template-rows: 9rem 1fr;
  background-color: var(--color-lightGrey);
`;
